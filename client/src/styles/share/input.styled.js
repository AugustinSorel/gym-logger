import { css } from "styled-components";

export const InputShare = css`
  font-size: var(--font-size-medium);
  padding: 1rem;
  border-color: var(--text-color);
  border: var(--border-height) solid;
  color: inherit;
  outline: none;
  border-radius: 24px;
  background-color: transparent;
`;

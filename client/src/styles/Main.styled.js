import styled from "styled-components";

export const Container = styled.main`
  flex: 1;
  position: relative;

  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 1rem;
  padding-left: 0;
`;

import theme from "../utils/theme";

const invalidInputVariants = {
  animate: {
    x: [0, -50, 50, 0],
    color: [
      theme.colors.textColor,
      theme.colors.InvalidInputColor,
      theme.colors.textColor,
    ],
  },
};

export default invalidInputVariants;
